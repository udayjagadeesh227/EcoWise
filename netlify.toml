[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true

# SPA fallback – placed after API rewrite so it does not capture API calls
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
